# format model input (intended for internal use)

format model input (intended for internal use)

## Usage

``` r
.generate_model_input(data, control_label, pseudocount = F)
```

## Arguments

- data:

  data

- control_label:

  control label

- pseudocount:

  logical for pseudocount
