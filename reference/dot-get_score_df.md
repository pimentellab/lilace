# get variant scores dataframe

get variant scores dataframe

## Usage

``` r
.get_score_df(lilace_obj, use_positions)
```

## Arguments

- lilace_obj:

  fitted Lilace object

- use_positions:

  whether Lilace was run with use_positions=T
