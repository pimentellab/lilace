# filter minimum total counts and zero count rows

filter minimum total counts and zero count rows

## Usage

``` r
.filter_data_low_counts(data, min_total_counts = 15)
```

## Arguments

- data:

  data

- min_total_counts:

  minimum total counts for variant
